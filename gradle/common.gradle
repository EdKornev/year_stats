ext {
    common = [
            compileSdkVersion : 29,
            minSdkVersion : 21,
            targetSdkVersion : 29,
            buildTools : '29.0.3',
            version : [
                    code : 1,
                    name : '0.0.1',
                    database: 1
            ]
    ]
}